# API Server Configuration
NEXT_PUBLIC_API_URL=http://localhost:4000

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
# IMPORTANT: Generate a secure secret using: openssl rand -base64 32
# A weak or changing secret will cause session loss on page refresh
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Environment
NODE_ENV=development

# SEO Verification Codes (Optional - for production)
# Get these from search engine webmaster tools after verification
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-google-verification-code
NEXT_PUBLIC_BING_SITE_VERIFICATION=your-bing-verification-code
NEXT_PUBLIC_YANDEX_VERIFICATION=your-yandex-verification-code
